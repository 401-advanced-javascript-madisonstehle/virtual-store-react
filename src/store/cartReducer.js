const initState = {
  cartContents: [],
  cartCount: 0,
}

const reducer = (state = initState, action) => {
  let newState = {...state}

  switch (action.type) {
    default:
      break;
  }

  return newState;
}

export default reducer;
